

export const metadata = {
    title: "Clip Url",
    description: "Shrink Your URL to your custom endpoint",
};

export default function RootLayout({ children }) {
    return (
        <html lang="en">
        <body >
            {children}
        </body>
        </html>
    );
}
